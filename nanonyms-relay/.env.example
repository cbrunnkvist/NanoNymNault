# Required - directory for OrbitDB persistent storage
ORBITDB_DATA_DIR=/path/to/storage

# Optional - network ports
LIBP2P_PORT=4001
WEBSOCKET_PORT=8081
HEALTH_PORT=3000

# Optional - OrbitDB database name (must match across all relays and wallets)
ORBITDB_NAME=nano-nym-alerts-v1

# Optional - bootstrap peers for relay-to-relay discovery
# Format: /dns4/relay.nanonym.org/tcp/4001/p2p/<peer-id>
# Comma-separated list for multiple peers
BOOTSTRAP_PEERS=

# Optional - logging level (debug, info, warn, error)
LOG_LEVEL=info
